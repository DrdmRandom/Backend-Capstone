version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PORT: "8010"
      DATABASE_URL: "sqlite:////data/solar_telemetry.db"
    ports:
      - "8010:8010"
    volumes:
      - sqlite_data:/data
    restart: always

volumes:
  sqlite_data:
