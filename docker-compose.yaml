version: "3.9"

services:
  api:
    build: .
    container_name: solar-fastapi
    ports:
      - "8001:8010"
    volumes:
      - .:/app
      - sqlite_data:/app
    restart: always

volumes:
  sqlite_data:
